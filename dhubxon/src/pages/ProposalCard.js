
import Card from './proposalcards';

const Proposal = () => {
  return (
    <div>
      <Card title="Total Balance" balance="150.000" color="yellow" />
      {/* Add more instances of Card component with different props as needed */}
    </div>
  );
};

export default Proposal;
